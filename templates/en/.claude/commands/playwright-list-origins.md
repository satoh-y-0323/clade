# /playwright-list-origins Command

Displays the current list of allowed origins for Playwright MCP.

## Execution Steps
1. Run the following using the Bash tool:
   ```
   node .claude/hooks/manage-playwright-origins.js list
   ```
2. Present the output directly to the user.
